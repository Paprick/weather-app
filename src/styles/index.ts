import { TileContainer } from "./tile-container.styled"
import { fadeInAnimation, fadeOutAnimation } from "./fade-animations.styled"
import { GlobalStyles } from "./global-style.styled"

export{
    TileContainer,
    fadeInAnimation,
    fadeOutAnimation,
    GlobalStyles
}